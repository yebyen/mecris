# Runbooks
* [Agent Session Bootstrap](agent-bootstrap.md) - Cold-start procedure for authenticating (`bin/mecris login`), loading lazy MCP families (`mecris_load_tools`), running the Gall loop (`/mecris-orient` → `/mecris-plan` → `/mecris-archive` → `/mecris-pr-test`), and working in a PR-protected repo. Includes UUID `c0a81a4b-115a-4eb6-bc2c-40908c58bf64`, `.venv` activation, `PYTHONPATH`, PocketID OAuth, `SIGINT`/`SIGTERM` handling, and pre-commit/CI validation steps.
* [Beeminder Emergency Response](beeminder-emergency.md) - Procedure for interpreting live goal risk (`derail_risk`, `safebuf`, `deadline`), selecting the smallest valid action before derailment, and recording durable discoveries per Beeminder's Midnight Mandate.
* [OKF Knowledge Base Maintenance](okf-maintenance.md) - Periodic tasks to keep the OKF bundle healthy: validate, check for stale concepts, add missing sources, and prune low-value nodes.
